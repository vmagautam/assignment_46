{
  "name": "client-management-backend",
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "start": "node src/server.js",
    "dev": "nodemon src/server.js",
    "migrate": "npx knex migrate:latest --env development",
    "seed:run": "knex seed:run --env development",
    "migrate:rollback": "knex migrate:rollback --env development",
    "migrate:rollback:all": "knex migrate:rollback --all --env development"
  },
  "dependencies": {
    "axios": "^1.13.2",
    "cors": "^2.8.5",
    "dotenv": "^16.3.1",
    "express": "^4.18.2",
    "knex": "^3.0.1",
    "pg": "^8.11.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.11",
    "wrangler": "^4.47.0"
  }
}
